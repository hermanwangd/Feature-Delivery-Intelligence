# Governance Recovery Boundary

There is **no approved active baseline inside this recovery bundle yet**. `CURRENT` is `NONE` by design.

The previous v0.4.8.0 `ACTIVE-AUTHORITY.md` was removed because recovery summaries must not become semantic authority.
HERM-219 must rehydrate/verify the exact approved normative source bytes, compute digests, build `GB-0001-CANDIDATE`, and obtain explicit approval before `CURRENT` can point to `GB-0001`.

Implementation/runtime files in this bundle are recovery candidates/scaffolds until that promotion gate passes.
