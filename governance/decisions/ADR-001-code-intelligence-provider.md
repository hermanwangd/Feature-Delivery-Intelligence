# ADR-001 — Code Intelligence Provider

**Decision:** Grafel is the preferred OSS implementation behind FDI `CodeIntelligenceProvider`.

Grafel produces pinned structural observations. It is not PA-03 itself, Product Realization authority, Product Knowledge authority, or current Feature truth. Provider-specific schemas/tool names must not leak into governing FDI contracts.
