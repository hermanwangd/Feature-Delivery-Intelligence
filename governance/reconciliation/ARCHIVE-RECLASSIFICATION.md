# Archive and Reclassification Record

- Layer 1 v0.3 is normalized under `candidates/HERM-219/` as
  `NON_GOVERNING_CANDIDATE`; it is not selected from an active normative path.
- PA-01 is normalized under `candidates/PA-01/` as `APPROVAL_CANDIDATE`; the
  merged-tree implementation copy remains source history, not semantic
  authority.
- Root `contracts/`, `skills/`, and `workflows/` are implementation surfaces.
  Their exact approved FT-T2 snapshot is pinned separately under
  `normative/ft-t2/`; bundle/runtime version labels confer no semantic authority.
- v0.7.1 and v0.8.0 bundle duplicates remain immutable external review
  snapshots and are not copied into active normative paths.
- `FDI-Layer1-Layer2-Governing-Spec-v1.0-APPROVED.md` was not present in the
  supplied v0.7.1 bundle, v0.8.0 bundle, governance handoff, or merge commit.
  No absent bytes were fabricated. If recovered, its required classification is
  `FDI-Layer1-Layer2-Consolidation-Candidate-v0.1`, `NON_GOVERNING`.

`governance/GOVERNING-SPEC.md` is the only generated consolidated view and is
verified byte-for-byte from baseline-selected modules.

