# Product Intelligence Repository Template

Copy this directory into a separate Product-owned Git repository such as:

```text
spc-product-intelligence.git
```

Do not store source-control credentials here.

Runtime `.fdi-work/` content is rebuildable and must remain outside durable Product Intelligence.
