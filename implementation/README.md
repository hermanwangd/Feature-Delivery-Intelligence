# Implementation Recovery Area — NON-NORMATIVE

Everything under `implementation/` is runtime/tooling/scaffold material. It cannot establish FDI semantic authority. Conformance claims are allowed only after an approved governing baseline exists in `governance/CURRENT` and the implementation is verified against its exact digests.
