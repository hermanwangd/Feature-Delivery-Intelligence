# FDI Multica Project Instructions

1. Read `/MULTICA-HANDOFF.md` and `/governance/approved-source-lock.json` first.
2. Use `/DEVELOPMENT-BACKLOG.md` as the current implementation sequence.
3. Do not merge/import the corrupted prior Multica project or the v0.4.8.1 recovery workspace into active paths.
4. Treat `specs/proposals/` as non-governing until explicit promotion.
5. Grafel is replaceable behind `CodeIntelligenceProvider`; Azure Repos must be frozen to exact local Git revisions before publication/evaluation use.
6. Durable Product Knowledge belongs in a separate Product-scoped Git-backed Product Intelligence Store.
7. Layer 2 / Structural Intelligence can generate candidates; current `CONFIRMED` / `EXCLUDED` and `SPEC_READY` require current feature-specific pinned Evidence.
