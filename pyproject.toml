[project]
name = "fdi-clean-project-baseline"
version = "0.4.8.2"
requires-python = ">=3.12"
dependencies = [
  "jsonschema>=4.26",
  "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
