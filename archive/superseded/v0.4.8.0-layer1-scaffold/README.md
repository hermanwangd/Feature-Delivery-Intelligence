# Layer 1 Binding

This recovery bundle does **not** invent replacements for the lost canonical Layer 1 implementation schemas.

The governing interface is frozen by name and semantics:

```text
T1 → T2 → T3 → T4
FT-T2 helper contracts = exact six
FT-T2 helper Skills = exact five
SPEC_READY | BLOCKED = sole T2 canonical gate
```

If an independently verified canonical Layer 1 package becomes available, bind/import it here without changing these invariants.

Until then, continuing work should focus on provider/source/Layer 2 recovery and preserve Layer 1 as an external canonical dependency.
