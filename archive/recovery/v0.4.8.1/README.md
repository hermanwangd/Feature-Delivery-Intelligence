# Recovery Origin v0.4.8.1

The v0.4.8.1 recovery workspace was a one-time reconciliation/hardening mechanism. Its active `REC-*` workflow is intentionally not part of this clean project baseline.

Hardening carried forward:
- Product Asset lifecycle/ref/registry fail-closed semantics;
- exact full Git revision binding;
- Grafel runtime/wire/adapter attestation and cross-field validation;
- provider-side structural query bounds plus post-result bounds;
- Azure Repos → exact local worktrees → Grafel boundary;
- no Grafel/Product Intelligence authority over current Feature truth.
