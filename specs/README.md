# Specifications

`specs/` is the normal project location for readable specification/conformance material.
Authority is selected by `governance/approved-source-lock.json`; these local views do not replace the external approved source bytes.
