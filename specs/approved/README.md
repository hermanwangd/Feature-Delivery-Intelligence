# Rehydrated Approved Source Bytes

This directory is the destination for exact approved normative source bytes when they are exported from the retained source library or original repository.

The clean project can continue **non-semantic implementation work** using the stable approved source identities pinned in `governance/approved-source-lock.json`.

Before any semantic spec mutation, successor approval, or canonical digest claim, rehydrate and digest at least:

- Layer 1 semantic spec v0.2 (`L1-SEM`)
- Layer 1 Markdown I/O profile v0.1 (`L1-IO`)
- HERM-211 FT-T2 locked package (`FT-T2`)
- Layer 2 Product Intelligence Framework v0.1 (`L2-FWK`)
- Product Asset Profile Specification v0.1 (`L2-PROFILE`, PA-03/PA-05 fully specified)
- PA Maintenance Skill Contracts v0.1 (`L2-MAINT`)

Do not reconstruct missing approved bytes from README/handoff prose.
