# Feature Delivery Intelligence — Clean Project Baseline v0.4.8.2

This is the new clean software-project baseline for continued Multica development.

## Architecture

```text
Product-owned Layer 2 Product Intelligence
          | ResolvedContextRefs
          v
Layer 1 Feature Development Execution
T1 Intention -> T2 Delivery Spec -> T3 Implementation -> T4 Correctness
```

Grafel is an optional/replaceable structural intelligence accelerator behind `CodeIntelligenceProvider`. Azure Repos is acquired into exact local Git worktrees. Durable Product Knowledge accumulates through governed Product Assets in a Product-owned Product Intelligence Store; MVP backend is Git.

## Project layout

```text
governance/   active development baseline, approved-source identity lock, ADRs
specs/        readable conformance views + product/structural designs
contracts/    executable JSON Schemas
skills/       Layer 1/FT-T2/Layer 2 implementation Skill surfaces
workflows/    maintain/bootstrap/develop workflows
src/fdi/      Python reference runtime
validation/   DEV-204/F001 protocols
config/       provider/store configuration examples
templates/    Product Intelligence repository template
scripts/      verification/build helpers
archive/      recovery/superseded history only
```

## Important status

This package is a clean **development** baseline. Approved semantic source identities are pinned, but original approved bytes are not embedded. Therefore implementation continuation is allowed, while governing semantic changes are blocked until byte rehydration/digest pinning.

Start with `MULTICA-HANDOFF.md` and `DEVELOPMENT-BACKLOG.md`.
